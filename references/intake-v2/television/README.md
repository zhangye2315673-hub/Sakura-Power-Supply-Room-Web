# Television v2 reference intake

Status: `conditional-fallback`.

The four images under `views/` are copied from the archived v1 model-review captures. They were not generated by GPT Image 2. They are admitted only as proportion, component-placement and palette evidence; the archived v1 rig contract remains authoritative for dimensions, pivots, sockets, collision and animation.

