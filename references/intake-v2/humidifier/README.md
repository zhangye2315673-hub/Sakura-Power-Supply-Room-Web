# Humidifier v2 reference intake

Status: `conditional-fallback`.

The four files under `views/` are copied from the archived v1 runtime captures after deterministic reference admission. They are not GPT Image 2 output. They provide silhouette, proportion, palette and component-placement evidence; the archived rig contract governs bounds, pivots, sockets, collision and animation.
