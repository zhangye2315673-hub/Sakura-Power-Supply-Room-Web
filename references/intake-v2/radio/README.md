# Radio v2 reference intake

Status: `conditional-fallback`.

The four images in `views/` are copied from the admitted existing radio crops/render. They were not generated by GPT Image 2 and are used only as form, proportion and palette evidence. Runtime dimensions, pivots, sockets, collision and animation remain governed by the archived v1 contract.
