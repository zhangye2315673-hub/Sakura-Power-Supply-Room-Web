# 录屏准备与家电音效撤回（2026-09-10）

## 本轮完成

- 家电只做视觉演出：移除逐台启动、运行、结束音频，以及家电落地提示音。保留家电模型、动画、灯光、技能规则。
- 保留背景音乐、夜间环境声和短操作音。可抽出时播放抽线反馈，接通时播放成功反馈；悬停不响，受阻不会先响抽线音。
- 同类操作音间隔至少 80ms，普通交互最多 8 个合成声部；完成、失败和模式开始反馈优先。静音或隐藏标签时清除待播交互声。
- 音频更新不再逐帧扫描家电或计算家电声像，环境音量仅在目标变化时更新。
- 01/02 试听页面、素材、生成脚本与修改前源码已移至 `artifacts/audio-rollback-20260910-145634`。没有删除素材；它们不再进入本轮构建的 `dist`。

## 录屏入口

- 本机：<http://127.0.0.1:5198/>
- 使用正式构建预览，不受开发热更新影响。恢复服务后，本机地址已重新验证 HTTP 200。
- 当前服务仅监听本机 127.0.0.1；之前的局域网地址不作为本次交付入口。
- 预览服务已启动；电脑关机或服务停止后需重新启动。这不是公网发布链接。
- 重新启动：`npm run build`，再执行 `npm run preview -- --host 0.0.0.0 --port 5198 --strictPort`。

## 验证结果

- `npm run build`：通过（含 TypeScript 检查）。
- `audio-feedback`、`opening-ui`、`skill-interaction-guards`：9 项通过。
- `theme-audio` 中家电视觉配置、材质、夜间灯光和质量切换：6 项通过。
- `node scripts/check-recording-readiness.mjs`：正式预览通过。实际进入游戏、连续抽线三次，剩余线束 5 → 4 → 3 → 2；静音持久化、恢复声音、打开图鉴、切换咖啡机并关闭图鉴正常。
- 390 × 844 窗口检查通过，无横向溢出；已检查桌面、图鉴和手机尺寸截图。此项不是手机真机触摸或帧率验证。
- 正式预览检查没有页面/控制台错误。声音诊断保持 `applianceAudioEnabled: false`，背景音乐播放时间正常推进。
- 原始报告和截图：`artifacts/recording-readiness-20260910/live-report.json` 及同目录 PNG。
- `git diff --check`：通过。未提交、未推送、未发布公网、未录制或发布视频。

## 后续建议（本轮未实施）

1. 首次加载：分段测量关卡生成、模型构建和资源请求，再决定延后哪些非首屏资源；不能把软件 WebGL 自动化的慢启动当成真实设备指标。
2. 新手引导：明确“拖动转视角、点击插头抽线”，先测试首次进入的理解成本，避免加常驻提示影响录屏画面。
3. 手机性能：优先用真实手机记录长时间游玩和多台家电同时演出的帧时间，再调整预算；本轮不声称提升了真实 FPS。
4. 代码整理：后续逐步拆分 Game 的生命周期和诊断逻辑，审计实际引用的音乐资源；录屏前不做大范围结构改动，也不删除无关素材。

## X 文案

和 5.6SOL 一起做了款 3D 插头解线小游戏 🔌 转一转，找出能抽出的线，让小家电一台台动起来。还在打磨中，先放一段实机视频，想听听大家的第一感觉！
